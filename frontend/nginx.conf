server {
    listen 8080;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Support Vue Router history mode by falling back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
